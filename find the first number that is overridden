public class Main {
    public static void main(String[] args) {
        int[] arr = {1, 4, 3, 2, 3};

        // Sort the array in ascending order
        Arrays.sort(arr);

        int n = arr.length;

        // Iterate through the sorted array
        for (int i = 0; i < n; i++) {
            // If the current element is not equal to its expected index,
            // it is the overridden number
            if (arr[i] != i + 1) {
                System.out.println(i + 1); // Print the overridden number
                break;
            }
        }
    }
}
